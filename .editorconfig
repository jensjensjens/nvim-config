root=true

[*]
charset=utf-8
end_of_line=lf
indent_size=2
indent_style=space
insert_final_newline=true
trim_trailing_whitespace=true

[*.md]
# Disable trim final newlines since two trailing whitespaces
# is a newline in markdown
trim_trailing_whitespace=false
